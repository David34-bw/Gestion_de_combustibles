# Servidor
server.port=8080

# Base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/fuel_control_db
spring.datasource.username=root
spring.datasource.password=tu_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT
jwt.secret=fuel_control_super_secret_key_2024_unipiloto_256bits
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# 2FA (tiempo de vida del código en minutos)
twofa.code.expiration=5

# CORS (permite el emulador Android)
cors.allowed-origins=http://10.0.2.2,http://localhost